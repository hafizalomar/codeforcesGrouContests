#include<stdio.h>

int main()
{
    int t;
    scanf("%d", &t);

    for (int i = 0; i < t; i++) {
        int operation;
        scanf("%d", &operation);

        int reminder = 0;

        while (operation != 0)
        {   
            if (operation%2 == 1) {
                
            }
            
        }

        printf("%d ", reminder);

    }

    return 0;
}